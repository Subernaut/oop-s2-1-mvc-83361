@model Library.Domain.Exam

@{
    ViewData["Title"] = "Delete Exam";
}

<h1 class="mb-4 text-danger"><i class="bi bi-exclamation-triangle-fill"></i> Delete Exam</h1>

<div class="card shadow-sm mb-4">
    <div class="card-body">
        <h5 class="card-title mb-3">Are you sure you want to delete this exam?</h5>
        <hr />
        <dl class="row mb-4">
            <dt class="col-sm-3 fw-bold">Title</dt>
            <dd class="col-sm-9">@Model.Title</dd>

            <dt class="col-sm-3 fw-bold">Course</dt>
            <dd class="col-sm-9">@Model.Course.Name</dd>

            <dt class="col-sm-3 fw-bold">Date</dt>
            <dd class="col-sm-9">@Model.Date.ToString("yyyy-MM-dd")</dd>

            <dt class="col-sm-3 fw-bold">Max Score</dt>
            <dd class="col-sm-9">@Model.MaxScore</dd>

            <dt class="col-sm-3 fw-bold">Results Released</dt>
            <dd class="col-sm-9">@Model.ResultsReleased</dd>
        </dl>

        <form asp-action="Delete" method="post" class="d-flex gap-2">
            <input type="hidden" asp-for="Id" />
            <button type="submit" class="btn btn-danger"><i class="bi bi-trash"></i> Delete</button>
            <a asp-action="Index" class="btn btn-secondary"><i class="bi bi-arrow-left"></i> Back to List</a>
        </form>
    </div>
</div>

@section Scripts {
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
}